<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home - User Stats</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <!-- include bootstrap -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #001f3f; /* Dark blue background */
      color: white; /* Default text color for contrast */
    }

    .card {
      background-color: #b3b5b5; /* Keep the card white */
      border: 4px solid #40E0D0;
      border-radius: 10px; /* Rounded corners */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Add a subtle shadow */
    }

    .overall-box {
      background-color: #8B0000; /* Dark red background */
      color: lightgray; /* White text for contrast */
      font-weight: bold; /* Make the text bold */
      padding: 5px 10px; /* Add some padding inside the box */
      border-radius: 5px; /* Rounded corners */
      display: inline-block; /* Ensure the box wraps only around the text */
    }
    .trophies-box {
      background-color: #dba400; /* gold background */
      color: lightgray; /* White text for contrast */
      font-weight: bold; /* Make the text bold */
      padding: 5px 10px; /* Add some padding inside the box */
      border-radius: 5px; /* Rounded corners */
      display: inline-block; /* Ensure the box wraps only around the text */
    }
    .money-box {
      background-color: #005913; /* gold background */
      color: lightgray; /* White text for contrast */
      font-weight: bold; /* Make the text bold */
      padding: 5px 10px; /* Add some padding inside the box */
      border-radius: 5px; /* Rounded corners */
      display: inline-block; /* Ensure the box wraps only around the text */
    }
    .tutorial-link {
      display: block; /* Make the link a block element */
      margin-top: 20px; /* Add spacing above the link */
      text-align: center; /* Center the link */
      color: white; /* White text for the link */
      font-weight: bold; /* Make the link text bold */
      text-decoration: none; /* Remove underline */
      padding: 10px 20px; /* Add padding to the link */
      background-color: #8134ed; /* Turquoise background */
      border-radius: 5px; /* Rounded corners */
      transition: background-color 0.3s ease; /* Smooth hover effect */
    }
    .tutorial-link:hover {
      background-color: #00509e; /* Darker blue on hover */
      color: white; /* Keep text white on hover */
    }
  </style>
</head>
<body>

<main class="container mt-4">
  <div class="card mx-auto" style="max-width: 500px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);">
    <div class="card-body text-center">
      <h1 class="card-title mb-4">Welcome, {{user.username}}</h1>
      <p class="card-text">
        <span class="overall-box">Your overall:</span> {{user.overall}} 🏀
      </p>
      <p class="card-text">
        <span class="trophies-box">Total Trophies:</span> {{user.trophies}} 🏆
      </p>
      <p class="card-text">
        <span class="money-box">Money:</span> {{user.money}} 💰
      </p>
      <a href="/tutorial" class="tutorial-link">Go to Tutorial</a>
    </div>
  </div>
</main>
  
</body>
</html>